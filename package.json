{
  "name": "vokabel-lern-begleiter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check learning.js && node --check store.js && node --check data.js && node --check sw.js"
  }
}
